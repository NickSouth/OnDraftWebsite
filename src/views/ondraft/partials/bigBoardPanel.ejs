<div id="big-board-heading-actions" class="big-board-heading-actions grid gap-3" hx-swap-oob="true">
  <%- include("bigBoardControls", { board, years, creators, positions, schools, filters }) %>
  <% if (isAdmin && board.creator !== "Consensus") { %>
    <a class="od-primary-link w-fit" href="/bigboard/edit?year=<%= board.year %>&creator=<%= board.creator %>">Edit board</a>
  <% } %>
</div>
<%- include("bigBoardResults", { bigBoard }) %>
