<section class="plain-page">
  <div class="page-heading">
    <div>
      <p class="eyebrow">Articles</p>
      <h1>Articles</h1>
    </div>
    <% if (isAdmin) { %>
      <a class="button-link" href="/articles/new">Create article</a>
    <% } %>
  </div>

  <% if (isAdmin) { %>
    <div class="article-status-toggle" aria-label="Article status">
      <button
        type="button"
        class="<%= showingPublished ? "is-active" : "" %>"
        hx-get="/articles/filter?status=published"
        hx-target="#articles-results"
        hx-swap="innerHTML"
        onclick="document.getElementById('article-status-field').value = 'published'; this.parentElement.querySelectorAll('button').forEach((button) => button.classList.remove('is-active')); this.classList.add('is-active');"
      >
        Published
      </button>
      <button
        type="button"
        class="<%= showingPublished ? "" : "is-active" %>"
        hx-get="/articles/filter?status=draft"
        hx-target="#articles-results"
        hx-swap="innerHTML"
        onclick="document.getElementById('article-status-field').value = 'draft'; this.parentElement.querySelectorAll('button').forEach((button) => button.classList.remove('is-active')); this.classList.add('is-active');"
      >
        Unpublished
      </button>
    </div>
  <% } %>

  <form
    class="article-filter-form"
    hx-get="/articles/filter"
    hx-target="#articles-results"
    hx-swap="innerHTML"
  >
    <% if (isAdmin) { %>
      <input id="article-status-field" type="hidden" name="status" value="<%= showingPublished ? "published" : "draft" %>" />
    <% } %>
    <div class="article-search-row">
      <label class="article-search-field">
        <span>Search</span>
        <input name="keyword" type="search" placeholder="Search articles" />
      </label>
      <button type="submit">Search</button>
      <button
        type="button"
        class="secondary-button"
        aria-expanded="false"
        aria-controls="article-filter-panel"
        onclick="const panel = document.getElementById('article-filter-panel'); const expanded = this.getAttribute('aria-expanded') === 'true'; this.setAttribute('aria-expanded', String(!expanded)); panel.hidden = expanded;"
      >
        Filters
      </button>
    </div>

    <div id="article-filter-panel" class="article-filter-panel" hidden>
      <label>
        Author
        <input name="author" />
      </label>
      <label>
        From
        <input type="date" name="dateFrom" />
      </label>
      <label>
        To
        <input type="date" name="dateTo" />
      </label>
      <label>
        Tags
        <input name="tags" />
      </label>
      <div class="article-filter-actions">
        <button type="submit">Apply filters</button>
        <button
          type="button"
          class="secondary-button"
          onclick="const status = document.getElementById('article-status-field')?.value || 'published'; this.form.reset(); const statusField = document.getElementById('article-status-field'); if (statusField) statusField.value = status; htmx.ajax('GET', `/articles/filter?status=${status}`, { target: '#articles-results', swap: 'innerHTML' });"
        >
          Clear
        </button>
      </div>
    </div>
  </form>

  <div id="articles-results">
    <% if (articles.length === 0) { %>
      <p>No articles have been created yet.</p>
    <% } else { %>
      <%- include("partials/articleList", { articles }) %>
    <% } %>
  </div>
</section>
